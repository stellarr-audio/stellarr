import { defineConfig } from 'astro/config';

// Minimal Astro config for Stellarr's landing page.
// Starlight + React will be added when we begin the docs migration.
export default defineConfig({
  site: 'https://stellarr.org',
});
